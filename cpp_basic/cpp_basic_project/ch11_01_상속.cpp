//클래스 상속
//기반 클래스 = 부모 클래스 (이미 작성된 클래스)
//파생 클래스 = 자식 클래스 (새로 작성하는 클래스)

// >> 이미 정의되어 있는 기반 클래스로부터 파생 클래스가 정의된다.

/*
class 파생클래스 : 접근지정자(주로 public) 기반클래스
{
	멤버함수 선언;
	멤버변수 선언;
};
*/

// 클래스 상속 : 이미 작성된 클래스와 새로 작성하는 클래스와의 관계를 결정하는 것
// 파생 클래스는 기반 클래스의 멤버를 사용할 수 있다.